<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <style>
       
       body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
        }
        header {
      background-color: #b04d42; /* Red background color */
      color: #df9aa9; /* White text color */
      padding: 10px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      
    }
  

    .logo {
      font-size: 24px;
    }

    .navbar {
      display: flex;
      justify-content: flex-end;
    }

    .navbar a {
      color: #ead8d8; /* White text color */
      text-decoration: none;
      padding: 10px;
      font-weight: light;
      margin: 2px;
    }

    .navbar a:hover {
      color: #ffffff; 
      background-color: #e65656; /* Darker red on hover */
      border-radius: 10px;
    }

        .profile-wrapper {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .profile-container {
            margin-top: 10px;
            width: 16%;
            /* Adjust the width as needed */
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
            margin-bottom: 20px;
        }

        .profile-picture {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            overflow: hidden;
            margin-bottom: 10px;
        }

        .profile-picture img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .user-info {
            margin-bottom: 20px;
        }

        .user-info h1 {
            margin: 0;
            font-size: 24px;
        }

        .user-info p {
            margin: 8px 0;
            font-size: 16px;
            color: #666;
        }

        .user-info a {
            text-decoration: none;
            color: #e1c6c6;
            font-weight: bold;
        }

        .logout-button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #e63724;
            /* Red background color */
            color: #fff;
            /* White text color */
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            font-size: 16px;
        }

        .logout-button:hover {
            background-color: #7d1a1a;
            /* Darker red on hover */
        }

        .upload-button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #245be6;
            /* Red background color */
            color: #fff;
            /* White text color */
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            font-size: 16px;
        }

        .upload-button:hover {
            background-color: #1a2f7d;
            /* Darker red on hover */
        }

        .card-container {
        display: flex;
        flex-wrap: wrap;
        
        justify-content: space-around; /* Adjust spacing between cards */
        margin-top: 20px; /* Add margin for separation from profile container */
    }

    .card {
        width: 250px;
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        margin: 16px;
        overflow: hidden;
        /* Removed flex-grow: 1; to prevent stretching */
    }

    .card img {
        width: 100%;
        height: 150px;
        object-fit: cover;
        border-radius: 8px 8px 0 0;
    }

    .card-caption {
        padding: 16px;
        text-align: center;
    }

    .upload-form {
    display: flex;
    align-items: center;
}

.caption-input {
    padding: 10px;
    margin-right: 10px; /* Add some spacing between the caption input and file input */
    box-sizing: border-box;
    border: 1px solid #ccc;
    border-radius: 5px;
    font-size: 14px;
    flex: 1; /* Take up remaining space */
}

.file-input-container {
    position: relative;
    overflow: hidden;
    margin-right: 10px; /* Add some spacing between the file input and the button */
}

.file-label {
    display: inline-block;
    padding: 10px 20px;
    background-color: #6f81ae;
    color: #fff;
    border-radius: 5px;
    cursor: pointer;
}
.file-label:hover{
    display: inline-block;
    padding: 10px 20px;
    background-color: #3c465e;
    color: #fff;
    border-radius: 5px;
    cursor: pointer;
}

.file-input {
    position: absolute;
    font-size: 100px;
    opacity: 0;
    right: 0;
    top: 0;
}

.upload-button {
    padding: 10px 20px;
    background-color: #3c8bd4;
    color: #fff;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 16px;
}

.upload-button:hover {
    background-color: #1a507d;
}


   

        
    </style>
</head>

<body>

    <header>
        <div class="logo">Pinterest</div>
        <nav class="navbar">
          <a href="/profile">Home</a>
          <a href="/feed">Feed</a>
          <a href="/logout" class="logout-button">Logout</a>
        </nav>
      </header>

    <div class="profile-wrapper">
        <div class="profile-container">
            <div class="profile-picture">
                <img src="https://srcwap.com/wp-content/uploads/2022/08/abstract-user-flat-4.png" alt="Profile Picture">
            </div>
            <div class="user-info">
                <h1><%= user.fullname %></h1>
                <p>@<%= user.username %></p>
                <p>Email: <%= user.email %></p>
                <p>Location: LA,USA</p>
               
                
            </div>

         
        </div>

        <form action="/upload" method="post" enctype="multipart/form-data" class="upload-form">
            <input type="text" name="imageText" placeholder="Add a caption..." class="caption-input">
            <div class="file-input-container">
                <label for="file" class="file-label">
                    Choose a file
                    <input type="file" name="file" id="file" class="file-input">
                </label>
            </div>
            <button type="submit" class="upload-button">Upload</button>
        </form>
        

        <!-- Card container for additional images -->
        <div class="card-container">
            
                <% user.posts.forEach((post)=>{%>
                    <div class="card">
                    <img src="images/uploads/<%= post.image%>"
                        alt="Card Image 1">
                    <div class="card-caption"><%= post.imageText%></div>
                </div>
                    <%})%>

            
           
            <!-- Add more cards as needed -->
        </div>
    </div>

</body>

</html>